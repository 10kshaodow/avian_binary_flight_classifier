# avian_binary_flight_classifier
Bird Flight Image Classifier — A computer vision project that classifies bird images as in flight or not in flight using transfer learning with deep CNNs. The pipeline includes dataset bootstrapping, automatic bird localization with YOLO-based cropping, and image classification with PyTorch.
